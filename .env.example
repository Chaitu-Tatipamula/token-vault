# PRIVATE_KEY=0x... (Your deployer private key)

# --- Production Deployment (HyperEVM Mainnet) ---
# Ensure these are real addresses on HyperEVM
# HYPEREVM_USDC_ADDRESS=0x...
# HYPEREVM_CORE_ADDRESS=0x...

# RPC URL for deployment
# HYPEREVM_RPC_URL=https://rpc.hyperliquid.xyz/evm  (Assuming valid RPC)

# If addresses are left commented out, the script will deploy MockUSDC and SKIP the HyperCore strategy.
